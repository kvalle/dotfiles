# Bat themes

`scripts/setup.sh` compiles custom themes into Bat's cache after installing the
dotfiles symlinks. To rebuild the cache manually, run:

```sh
bat cache --build
```

Run the command again after changing a file under `bat/themes/`.
