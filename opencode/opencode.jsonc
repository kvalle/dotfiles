{
  "$schema": "https://opencode.ai/config.json",
  // Oppdateringer håndteres av brew - notify minner oss på å kjøre 'brew upgrade'
  "autoupdate": "notify",
  // LSP gir agenten IDE-lignende kodenavigasjon (go-to-def, references, diagnostics)
  "lsp": true
}
