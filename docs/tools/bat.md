# Bat themes

`scripts/setup.sh` builds the custom themes into Bat's cache after the dotfiles
symlinks are installed. Build the cache manually with:

```sh
bat cache --build
```

Re-run the command after changing anything under `bat/themes/`.
