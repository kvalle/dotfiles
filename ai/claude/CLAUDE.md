- Ask clarifying questions as plain written text in the reply — never via the
  AskUserQuestion tool's structured UI. This is about format, not frequency:
  still ask whenever something is genuinely the user's call to make.
- In Plan Mode, do not call ExitPlanMode as soon as a draft plan is ready.
  First present the plan's key content as normal chat text in the
  conversation and let the user discuss and adjust it there. Only call
  ExitPlanMode after the user has given explicit approval in the chat (e.g.
  "ja", "ok", "kjør på") — never as the automatic next step right after
  drafting.
